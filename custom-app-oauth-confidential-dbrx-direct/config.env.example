# Databricks Direct OAuth Configuration
# Direct authentication with Databricks OAuth endpoints (no external IdP)

# Application Configuration
PORT=8001
FLASK_DEBUG=True
FLASK_SECRET_KEY=databricks-direct-oauth-app-secret

# Databricks OAuth Configuration (Confidential Client)
DATABRICKS_SERVER_HOSTNAME=your-workspace.cloud.databricks.com
CLIENT_ID=your-databricks-oauth-client-id
CLIENT_SECRET=your-databricks-oauth-client-secret
REDIRECT_URI=http://localhost:8001/callback
OAUTH_SCOPE=all-apis offline_access

# Databricks SQL Endpoint Configuration
DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/your-warehouse-id

# Note: DATABRICKS_HTTP_PATH is optional - users can specify warehouse ID in the UI




