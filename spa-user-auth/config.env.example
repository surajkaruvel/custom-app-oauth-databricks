# User Authentication Configuration - Okta SPA with PKCE
# Users authenticate via Okta and queries execute as their user account

# Application Configuration
PORT=5000
FLASK_DEBUG=True
FLASK_SECRET_KEY=user-auth-oauth-app-secret

# Okta SPA Configuration (PKCE Flow - No client secret needed)
ISSUER_URL=https://your-okta-domain.okta.com/oauth2/your-auth-server-id
CLIENT_ID=your-spa-client-id-here
REDIRECT_URI=http://localhost:5000/callback
OAUTH_SCOPE=openid profile email all-apis offline_access

# Databricks SQL Endpoint Configuration
DATABRICKS_SERVER_HOSTNAME=your-workspace.cloud.databricks.com
DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/your-warehouse-id

# Note: DATABRICKS_HTTP_PATH is optional - users can specify warehouse ID in the UI
